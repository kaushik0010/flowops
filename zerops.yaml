zerops:
  - setup: app
    run:
      base: nodejs@20
      start: node server.js
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        HOSTNAME: 0.0.0.0
        NODE_ENV: production
        PORT: "3000"